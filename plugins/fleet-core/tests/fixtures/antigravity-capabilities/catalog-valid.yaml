{
  "catalog_schema": "antigravity.capability-catalog.v1",
  "receipt_schema": "antigravity.capabilities.v1",
  "catalog_revision": 1,
  "capabilities": [
    {
      "id": "agy.agent.execution",
      "revision": 1,
      "description": "Prove independent agent execution.",
      "probe_method": "controlled-agent-execution",
      "probe_revision": 1,
      "expected_evidence": ["agent-execution-proof"],
      "required_for": ["live-canary"],
      "outcome_rules": {
        "passed": "independent-execution-proven",
        "failed": "independent-execution-refuted",
        "unknown": "independent-execution-unobserved",
        "unavailable": "controlled-fixture-unavailable"
      },
      "fallback": {
        "capability": "agy.sequential.isolation",
        "for_consumers": ["saga.ideate"],
        "when_states": ["unknown", "unavailable"]
      }
    },
    {
      "id": "agy.sequential.isolation",
      "revision": 1,
      "description": "Prove isolated sequential execution.",
      "probe_method": "controlled-sequential-isolation",
      "probe_revision": 1,
      "expected_evidence": ["sequential-isolation-proof"],
      "required_for": [],
      "outcome_rules": {
        "passed": "sequential-isolation-proven",
        "failed": "sequential-isolation-broken",
        "unknown": "sequential-isolation-unobserved",
        "unavailable": "controlled-fixture-unavailable"
      },
      "fallback": null
    }
  ]
}
