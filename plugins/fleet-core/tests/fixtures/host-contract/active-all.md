Write the execution marker to `.claude/delegation/active.json`.
Call `AskUserQuestion` after loading it with `ToolSearch`.
Run `Workflow("source-only")` for the implementation.
Read sessions from `.gemini/antigravity-cli/brain`.
The engine automatically schedules the run.
The backend guarantees isolated worktree execution.
